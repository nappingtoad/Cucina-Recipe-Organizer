# Cucina Recipe Organizer

A recipe organizer that allows users to manage their favorite recipes.

## Running the code

Run `npm install` to install the dependencies.

Run `npm run dev` to start the development server.
  
